import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 've.gob.fona.recosup',
  appName: 'RECOSUP',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
